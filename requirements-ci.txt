# Lightweight dependencies for CI (lint + unit + integration tests).
# Excludes torch/transformers/peft — tests use fakes/mocks for ML components.
requests>=2.31.0
pyyaml>=6.0.0
numpy>=1.26.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic>=2.5.0
prometheus-client>=0.20.0
httpx>=0.26.0
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
