# =============================================================================
# Na'vi Language Translator — Python Dependencies (Runtime / API Serving)
# =============================================================================

# --- Core ML (CPU-only to keep image small) ---
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.36.0
peft>=0.7.0              # LoRA adapters for Whisper
datasets>=2.16.0
sacrebleu>=2.3.0          # BLEU score computation
jiwer>=3.0.0              # WER computation
sentencepiece>=0.1.99     # MarianMT tokenizer

# --- Audio Processing ---
librosa>=0.10.0
soundfile>=0.12.0

# --- TTS Bootstrap ---
gTTS>=2.5.0

# --- Data & API ---
requests>=2.31.0
pyyaml>=6.0.0
pandas>=2.1.0
numpy>=1.26.0

# --- API Serving ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6   # file uploads
pydantic>=2.5.0

# --- Monitoring (lightweight) ---
prometheus-client>=0.20.0

# --- MLOps (runtime tracking only) ---
mlflow==3.11.1
