# =============================================================================
# Na'vi Language Translator — Dev / MLOps Dependencies
# (NOT needed in the production Docker container)
# =============================================================================

-r requirements.txt

# --- MLOps / Orchestration ---
dvc>=3.40.0
apache-airflow>=2.8.0
great-expectations>=0.18.0
evidently>=0.4.0

# --- Testing ---
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
httpx>=0.26.0             # async test client for FastAPI

# --- Linting ---
ruff>=0.2.0
